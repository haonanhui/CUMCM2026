用序列区间 [π(o ),π(z )) 表示第 j 段物理驻留，从 ALLOC 或 SPILL_IN 开始，到
bj bj
FREE或SPILL_OUT结束。其地址为整数半开区间[x ,x +s )，满足
bj bj b
x ∈ Z , x +s ≤ C . (10)
bj ≥0 bj b t
b
若两个同类型驻留段在序列上重叠，其地址必须满足析取约束
x +s ≤ x 或 x +s ≤ x . (11)
bj b ak ak a bj
每个使用者必须落在相应驻留段内，段的关闭节点等待该段全部使用者，下一次换入等待
对应换出。时间上的地址互斥还由下一节的释放到再占用依赖保证；仅在输出序列上画不
重叠区间，并不足以证明并行执行时安全。
6.2 搬运目标与连续空间的困难
令χ 按题面COPY_IN引用关系取值。问题二的目标是
b
X
minD = (2−χ )s , (12)
b b
(b,j)∈M
约束为原始节点拓扑顺序、式 (9)–(11) 及换出换入配对。重复换出同一对象按不同事件重
复计费。一次换入耗时2s +150；换出在χ = 1时耗时零，否则也是2s +150。这些耗时
b b b
进入问题三的执行单元工作量，零耗时换出仍是依赖节点。
容量和连续性是两类约束。以容量10为例，若两个当前需保留的对象位于[0,2)、[6,8)，
剩余空闲区间长4和2；新对象长5，三者总长仅为9，却没有一个空闲段可容纳它。因而
“空闲总量足够”不是分配成功的充分条件。动态存储分配文献将空闲区间组织与外部碎片
作为核心问题[2]，本文据此同时检查空闲总量与可用连续区间。
记某类型当前最大连续空闲段长度为Lmax，空闲总量为F 。请求大小为s时，固定现
t t
有偏移下的可分配条件是 Lmax ≥ s；若 F ≥ s > Lmax，阻塞来自外部碎片；若 F < s，则
t t t t
空闲总量已经不足。两种情形都可能通过换出释放空间，但牺牲对象的位置决定释放后能
否合并成足够长的区间。后文的距离评分按对象选牺牲者，连续区间扫描负责确认每次牺
牲是否已经达到分配条件。
最佳适配枚举所有长度不小于 s 的空闲段，选择长度最小的一段，并从该段起始位置
分配；长度相同再取较低地址。其直接效果是使本次分配在所选空闲段内留下的余量较小，
较长空闲段可以暂时保留给后续较大对象。未来申请和释放可能改变区间结构，因此每次
仍重新计算空闲段。由此，地址选择是基于当前状态的启发式，与完整调度顺序共同决定
最终搬运量。
6.3 分配、换出与恢复算法
先沿原始顺序预存每个对象的未来使用位置队列，再逐节点模拟四状态迁移。遇到申
请或换入，扫描对应缓存，优先选择长度最小且足够的空闲区间。若没有可用区间，排除当
前操作必需的对象，从其余同类型驻留对象中选择牺牲者，插入换出并合并空闲区间，重
复直至分配成功。
12