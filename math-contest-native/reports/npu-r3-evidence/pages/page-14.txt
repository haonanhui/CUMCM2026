牺牲评分比较两种选择：最大化下一次使用距离∆ ，或最大化
b
∆
qspill = b . (13)
b (2−χ )s
b b
无后续普通操作使用的对象，将其距离置为原始序列长度加一。后一规则倾向于换出未来较
晚使用且搬运较便宜的对象，连续空闲段的组合效果则在每次换出后重新扫描确认。Bélády
对替换问题的研究利用完整未来引用序列构造离线比较基准 [3]。本文借鉴未来引用信息，
在对象大小不等、多对象同时使用及连续地址约束下构造上述两种牺牲评分，并以六图真
实搬运量比较其表现。
普通换出可能被受保护对象的碎片阻塞，此时启用显式恢复：换出本次操作所需缓存
类型中的全部驻留对象，再按对象大小递减顺序重装本次必需集合。该步骤允许改变偏移，
但每次搬运均计入式(12)，不是零成本地址搬移。当前实现以容量单元数组表示占用情况，
便于检查边界；这也是容量不大时选择简单实现的原因。
恢复成功的前提为每个操作在每种缓存上的必需总量满足
X
s ≤ C , v ∈ V , t ∈ T . (14)
b t o
b∈Bv:t
b
=t
这一前提已对六图逐操作检查。证明如下：将相关类型清空后，在同一类型中把必需对象
紧邻排列，其最后一个地址不超过总长，而总长不超过容量，所以存在连续放置。未换入
的其他对象可留在DDR等待后续使用；DDR容量在题面中不作限制。在固定原始顺序下，
每个节点均可通过有限次换出和重装推进，因而不会因外部碎片永久停滞。若式(14)失败，
单纯换出策略无法解决同时使用需求，应直接报告不可行，而不是无限循环。
初始偏移对应原始ALLOC，每次换出另记录新的换入偏移。若有M 次换出，新增节
点恰为 2M 个，第 j 次从零编号时使用 N +2j,N +2j +1，并保持原始节点各出现一次。
未来使用队列的预处理为 O(N +Q)。计入容量扫描、牺牲对象扫描及恢复时的对象排序，
分配阶段的保守时间界为 O((N + M)(C + |B| + Klog(K + 1)) + Q)，其中 C = max C ；
t t
显式恢复增加的工作已包含在实际M 内，不将其当作常数开销。
6.4 一次分配的决策计算与恢复示例
未来使用信息来自已经给定的原始节点顺序。设当前处理位置为i，对象b的未来使用
队列首项为 j ，则 ∆ = j −i。处理完当前操作后，弹出本次使用位置；以后即使对象暂
b b b
时换出，其未来队列仍继续保留。这样，逻辑需求与物理状态相互独立，换入时能够找到
下一使用需求，而无需重新扫描整个后缀。
两种牺牲规则的差别可以用一个构造状态说明。设同类型两个可换出对象 a,b 的大小
分别为4、2，下一使用距离分别为12、8；a为题面输入复制对象，b为中间对象。两者一
次事件的搬运代价均为4，距离评分和代价评分都优先换出a。若把a改为中间对象，其代
价变为 8，代价评分变为 12/8 = 1.5，而 b 的评分为 8/4 = 2，此时距离规则仍选择 a，代
价规则选择b。这一计算说明，分母改变的是在未来距离与本次搬运之间的取舍。
牺牲规则还受到保护集合与地址形状的共同限制。若当前操作同时使用 a，它就从普
通牺牲集合中排除，即使其评分最大也不会被普通换出选中。若换出b后仍无足够长区间，
13