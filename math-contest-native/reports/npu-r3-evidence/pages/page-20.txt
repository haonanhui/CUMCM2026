换入节点还必须等待对应换出。对于一个对象的多次换出，每段的使用者均在该段开
始之后、结束之前，相邻段由换出到换入的配对关系连接。由这些路径可以推出早期使用
先于后续换出、早期换入先于后续使用，逐段构造因而覆盖整个逻辑生命。原始依赖边同
时保留，物理地址约束是在原图基础上增加的限制。
再考虑两个操作的手算例。输入搬运单元执行 u，耗时 10；输出搬运单元执行 v，耗
时 20，原图中二者无依赖。若两对象使用不同地址，则 S = S = 0，完成时间为 20。若
u v
它们先后复用同一区间，旧段释放到新段申请的边使v 等待u，此时S = 10，完成时间为
v
30。这两个结果已经通过独立重放检验，差别全部来自地址关系。
该例还说明零时长管理节点的作用。申请与释放本身不增加工作量，却能把一段计算
的完成约束传给下一段。删除零时长节点时必须保留其传递关系；否则可能错误地得到两
个占据相同地址的操作并行执行。本文保留管理节点参加最长路递推，使地址安全与时间
计算使用同一组结构边。
7.4 候选扩展与列表调度
首先扩展原始候选：增加两种按估计最早开始时间与新增容量加权的操作排序，窗口
均为64，新增容量权重分别为0、1周期/容量单位；对八种顺序的距离换出方案分别尝试地
址轮换。轮换从上次分配末端继续查找空闲区间，以改变地址复用形成的依赖。它同时可
能改变碎片和换出次数，因而不能声称轮换天然保持搬运不变。初始候选共二十四个，仅
保留满足式(15)者，从中取得时间最小方案。
其次分别以问题二方案及扩展候选的最佳时间方案为起点，构造固定驻留图。按逆拓
扑序计算
r = c + max r , (18)
v v u
u:(v,u)∈ER
无后继时最大项取零。每个执行单元维护以r 降序排列的就绪队列，管理节点就绪后立即
v
处理。比较两种选择：其一在各队首中选择估计可开始时间最早者，同分优先较大的r ；其
v
二直接优先较大的r 。新顺序必须再次通过完整依赖与地址重放，只有真实T 下降才替换
v
原方案。
调度优先级借鉴HEFT的向上秩思想[4]。本题将执行单元作为节点的给定属性，以固
定节点时长和驻留依赖计算式(18)。较大的r 表示从当前节点出发还有较长的依赖工作链，
v
优先安排它有助于避免长链持续推迟。本文在既定执行单元上采用追加式列表调度，性能
由本题候选的完整重放评价。
设N′ = N +2M。路径计算与边遍历为O(N′ +|E |)，优先队列维护为O(N′logN′)；
R
固定数量单元的队首比较为 O(N′)。若计入逐单元地址依赖构造，还需 O(N′C) 的保守扫
描开销。该策略没有回溯改变已保留的地址链，所以算法高效但搜索受限。
7.5 列表调度的执行步骤与候选接受
构造驻留图后，首先计算各节点入度与下游秩，并把入度为零的节点加入其所属单元
的优先队列。管理节点使用单独的就绪队列；它们一旦满足结构前驱，就按已有前驱完成
19