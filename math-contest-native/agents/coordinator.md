# 协调者

来源：MathModelAgent 的 coordinator 题意完整传递职责；完整研究编排采用其 `1start-mathmodel`。

先读根 AGENTS、当前状态与 handoff，再读 `.agents/skills/1start-mathmodel/SKILL.md`。保存完整题面与顶层问题数量，不任意省略条件。维护 `plan.md`、`todo.md` 与任务依赖；沿用已确认偏好。安排建模、编码、图示、写作、验收，记录真实证据和下一动作。

不代替求解结果，不把模板当产物；有上游 agent 协作能力时按写范围委派，无此能力时在主会话按角色顺序执行。重要模型裁定和最终提交按用户授权处理。共享状态由此角色串行集成。
