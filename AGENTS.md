# 复杂系统数学建模与数值计算智能体协作指引 (AGENTS.md)

本指引适用于参与本项目协同研发的各类 AI 智能体（Codex、Antigravity、Claude、ChatGPT、Cursor 等）。
本项目为 **复杂工程系统数学建模与数值仿真优化联合研究课题**，采用 **“GitHub 私有仓库承载共享源码和持续协作，Release 承载精简部署包”** 的高效研发模式。

## 1. 核心原则与工程边界

- **项目根自包含**：所有代码、配置与数据路径必须推导自当前工作区根目录（通过相对路径），严禁在任何脚本或配置文件中硬编码特定机器的绝对路径。
- **无中央治理依赖**：本项目独立自洽，不依赖维护者机器的私有环境、全局配置或特定框架。
- **零凭证泄漏**：严禁将任何 API Key、Token、密码、代理配置或 `.env` 文件写入代码或提交至 Git。
- **不可变运行结果**：任何计算与仿真输出必须放入独立的运行目录，严禁覆盖历史运行目录或其他队员的成果。
- **谨慎 Git 提交**：严禁直接使用未经筛选的 `git add .`。每次提交前必须检查 staged diff，确保仅提交授权和必要的文件。
- **Vendor 资产只读**：`code/vendor/` 为引用的第三方科学计算基线工具与算法库，一律保持只读，禁止原地修改。

## 2. 团队槽位与职责划分

| 槽位 | 角色定位 | 核心工作 |
|---|---|---|
| `member-01` | 课题统筹与集成人 | 维护模型技术合同、分配研发任务卡、单写者发布正式指标 (`frozen_numbers.json`)、统筹技术报告大纲与成果汇编 |
| `member-02` | 数据算法与数值计算 | 实验数据清洗、数学机理算法设计与数值求解、收敛性与敏感度分析、产出独立运行目录及 `result.json` |
| `member-03` | 报告撰写与独立核验 | 学术报告排版、图表绘制与嵌入、物理量纲/数学约束/公式独立核查、学术诚信规范核对 |

## 3. 标准任务工作流

1. **认领任务**：
   - 检查 `team/tasks/<task_id>.json`，确认研发目标、依赖交付物与输入数据。
   - 创建或切换至任务分支：`git checkout -b work/<member_id>/<task_id>`。

2. **本地环境**：
   - 使用项目局部虚拟环境 `.venv` 运行 Python 代码。
   - 核心流水线自检入口：`python code/pipeline/tests/smoke_test.py`。

3. **代码编写与独立求解**：
   - 核心算法与数值求解脚本放置于 `code/`（例如 `code/models/<task_id>/`）。
   - 计算与仿真产物统一写入专属运行目录：  
     `data/contest/<contest>/<problem>/04_solve/runs/<member_id>-<task_id>-<run_id>/`
   - 必须在运行目录下生成标准元数据 `result.json`（参考 `team/templates/result.example.json`），记录实验参数、指标数值、复现命令行与验证状态。

4. **学术诚信与辅助边界**：
   - 团队统一遵循 [docs/AI_USAGE_STATEMENT.md](docs/AI_USAGE_STATEMENT.md) 固定声明：AI 辅助聚焦于语言润色、格式校对与背景资料检索，所有数学建模与算法求解均由团队自主推进。无需针对每次交互进行繁琐登记。

5. **提交与交接**：
   - 明确暂存并提交修改的文件：`git add code/models/... team/tasks/...`
   - 提交信息格式：`feat(<task_id>): [member_id] 简述交付内容`
   - 推送分支并通知当班集成人集成。

6. **正式指标发布与报告引用**：
   - 当班集成人（member-01）统一将已核验的关键指标写入 `04_solve/frozen_numbers.json`。
   - 学术报告源稿统一使用占位符 `{{NUM:metric_key}}`，严禁手工填入未冻结字面量。
